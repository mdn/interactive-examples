var cmInitContent = `// create your array
var arr1 = ['alpha', 'bravo', 'charlie', 'delta', 'echo', 'foxtrot', 'golf'];

/* call copyWithin passing the target, starting,
and ending points */
var result = arr1.copyWithin(2, 4, 6);

// log the result
console.log(result);`;

var cmSelectLine = 5;
var cmSelectChStart = 16;
