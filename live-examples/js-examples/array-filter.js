var cmInitContent = `// create an array
var words = ['spray', 'limit', 'elite', 'exuberant', 'destruction', 'present'];

// Run filter against the array
var result = words.filter(function(word) {
    return word.length > 6;
});

// log the result
console.log(result);`;

var cmSelectLine = 1;
var cmSelectChStart = 77;
